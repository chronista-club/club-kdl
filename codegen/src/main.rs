//! `club-kdl-codegen` CLI — the auxiliary entry point for manual generation
//! and CI verification.
//!
//! The primary entry point is the library API ([`kdl_codegen`]), which
//! consumers such as club-unison call from their `build.rs`. This CLI is a
//! thin wrapper for ad-hoc use.
//!
//! Not yet implemented — see `ROADMAP.md` (Phase 1).

fn main() {
    eprintln!("club-kdl-codegen: CLI not yet implemented (Phase 1, scaffold stage)");
    std::process::exit(1);
}
