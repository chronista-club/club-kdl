[package]
name = "unison-kdl"
version = "0.1.0"
edition = "2021"
description = "A KDL (KDL Document Language) parser with serde support"
license = "MIT"
repository = "https://github.com/chronista-club/unison-kdl"
keywords = ["kdl", "parser", "serde", "config"]
categories = ["parser-implementations", "config"]

[dependencies]
thiserror = "2"

[dev-dependencies]
pretty_assertions = "1"
